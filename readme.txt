1- docker ile minio kurulumu(user ve şifre biz veriyoruz)
2- kurulum yaptıktan sonra minio sitesi üzerinden accessKey ve secretKey alıyoruz.
3- minio sitesi üzerinden bucket oluşturduk
4- config kısımına access, secretkey ve bucket isimini verip,  multer kısımını yazdık.

